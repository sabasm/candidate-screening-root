{
  "name": "candidate-screening-root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "scripts/start-dev.sh",
    "build": "cd app && npm run build",
    "start": "cd app && npm run start",
    "test": "jest && pytest llm/tests/"
  }
}
